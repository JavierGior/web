El usuario pidió pushear al repositorio https://github.com/JavierGior/web. Necesito los comandos exactos y secuenciales para: 1) verificar si hay cambios sin commitear, 2) verificar si el remoto 'origin' ya apunta a esa URL o si hay que configurarlo, 3) hacer el push. Asumí que el repo local ya existe y tiene al menos un commit. Si el remoto no está configurado, incluí el comando para agregarlo. Si la rama actual no tiene upstream configurado, incluí el flag correspondiente.